package advance.algo.and.data.struct.book;

/**
 * Increase the performance of find operation of a list
 * Level 1 skips every other node
 * Level 2 skips every other node from level 1
 * Level X would skip every other node from level x-1
 * Levels are stopped once distance between nodes reaches N/2-1
 */
public class SkipList {
}
